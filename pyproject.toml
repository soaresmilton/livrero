[project]
name = "livrero"
version = "0.1.0"
description = "Livrero - Personal Reading OS"
requires-python = ">=3.13"
dependencies = ["livrero-api"]

[tool.uv.sources]
livrero-api = { workspace = true }

[tool.uv.workspace]
members = ["apps/api"]
